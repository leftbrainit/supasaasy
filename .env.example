# SupaSaaSy Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# Supabase Configuration (Required)
# =============================================================================

# Your Supabase project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase anon/public key (safe to expose in client-side code)
SUPABASE_ANON_KEY=your-anon-key

# Supabase service role key (keep secret, server-side only)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =============================================================================
# Admin API (Required for manual sync endpoints)
# =============================================================================

# API key for authenticating admin requests (generate a secure random string)
SUPASAASY_ADMIN_API_KEY=your-admin-api-key

# =============================================================================
# Stripe Connector (if using Stripe)
# =============================================================================

# Stripe API key for the production app instance
# STRIPE_API_KEY_STRIPE_PRODUCTION=sk_live_...

# Stripe webhook signing secret for the production app instance
# STRIPE_WEBHOOK_SECRET_STRIPE_PRODUCTION=whsec_...

# =============================================================================
# Additional Connectors
# =============================================================================

# Add environment variables for other connectors as needed
# Follow the pattern: {PROVIDER}_{VARIABLE}_{APP_KEY}
